#include <pazzers/geometry/rectangle.hxx>

namespace pazzers
{
    namespace geometry
    {
        Rectangle::Rectangle(int x, int y, int width, int height):
            x(x),
            y(y),
            width(width),
            height(height)
        {

        }
    }
}
